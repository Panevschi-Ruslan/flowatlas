note "a browser holds a stream open, opened through a wrapper two calls away"
say "flowatlas impact entry:api:http:GET:/depots/:param/events --format tree" 4
note "every subscription, and the screen where its address was really written"
pause 3
