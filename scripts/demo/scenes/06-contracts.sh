note "what each service sends, against what the other side declares"
say "flowatlas contracts --severity error --max-nodes 4" 5
note "the same name, declared two ways, in two repositories"
say "flowatlas types --drift --format tree" 4
pause 2
