// Hand-written `typeorm` stub for the flowatlas fixture suite. It declares only what
// `fixtures/nest-typeorm/src` touches, shaped like the real library so that
// `resolveTypeOrigin` sees the same thing it sees on a real repo: the receiver's type
// is declared in `node_modules/typeorm`, and the entity is the first type argument of
// `Repository<Entity>`.
//
// `InjectRepository` really lives in `@nestjs/typeorm`; it is folded in here so the
// fixture needs a single stub package. That does not affect the adapter: the decorator
// is never a call receiver.

export declare class Repository<Entity> {
  find(options?: unknown): Promise<Entity[]>;
  findOne(options?: unknown): Promise<Entity | null>;
  save(entity: Entity): Promise<Entity>;
  delete(id: unknown): Promise<void>;
  createQueryBuilder(alias?: string): unknown;
}

export declare const Entity: (name?: string) => ClassDecorator;
export declare const PrimaryGeneratedColumn: (strategy?: string) => PropertyDecorator;
export declare const Column: (options?: unknown) => PropertyDecorator;
export declare const InjectRepository: (entity: unknown) => any;
