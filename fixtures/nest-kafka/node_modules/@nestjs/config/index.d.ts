// Hand-written `@nestjs/config` stub, the same shape the `nest-leaves` fixture
// uses. It is here so the `config.get` step of the channel-name resolver
// (step 3 of four) has a receiver whose type origin is a real package.

export declare class ConfigService {
  get<Value = string>(key: string): Value | undefined;
  get<Value = string>(key: string, defaultValue: Value): Value;
  getOrThrow<Value = string>(key: string): Value;
}
