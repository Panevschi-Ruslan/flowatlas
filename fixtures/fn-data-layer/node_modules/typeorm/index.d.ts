// Hand-written `typeorm` stub for the flowatlas fixture suite. It declares only what
// `fixtures/fn-data-layer/src` touches, shaped like the real library so that
// `resolveTypeOrigin` sees the same thing it sees on a real repo: the receiver's type is
// declared in `node_modules/typeorm`, and the entity is the first type argument of
// `Repository<Entity>`.
//
// A `DataSource` rather than an injected repository, because that is how code without a
// container gets hold of one, and code without a container is what this fixture is about.

export declare class Repository<Entity> {
  find(options?: unknown): Promise<Entity[]>;
  save(entity: Entity): Promise<Entity>;
  delete(id: unknown): Promise<void>;
}

export declare class DataSource {
  getRepository<Entity>(target: unknown): Repository<Entity>;
}

export declare const Entity: (name?: string) => ClassDecorator;
export declare const PrimaryGeneratedColumn: (strategy?: string) => PropertyDecorator;
export declare const Column: (options?: unknown) => PropertyDecorator;
