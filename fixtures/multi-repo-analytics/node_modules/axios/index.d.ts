// Hand-written `axios` stub.

export interface AxiosResponse<Data = unknown> {
  data: Data;
  status: number;
}

export declare const axios: {
  get<Data = unknown>(url: string, config?: unknown): Promise<AxiosResponse<Data>>;
  post<Data = unknown>(url: string, body?: unknown, config?: unknown): Promise<AxiosResponse<Data>>;
  delete<Data = unknown>(url: string, config?: unknown): Promise<AxiosResponse<Data>>;
};

export default axios;
