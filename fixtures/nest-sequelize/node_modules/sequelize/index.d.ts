// Hand-written `sequelize` stub for the flowatlas fixture suite. It declares only what
// `fixtures/nest-sequelize/src` touches, shaped like the real library so that
// `resolveTypeOrigin` sees the same thing it sees on a real repo.
//
// Sequelize is called on the model itself rather than on an injected repository, which
// is the shape this fixture is here for: the receiver is a class of the repository, and
// the only reason it is data access at all is that the class extends a class declared in
// `node_modules/sequelize`. `packagedBaseOf` is what carries the origin across that
// inheritance, and the table is whatever `init` or `define` stated — in no type.

export declare const DataTypes: {
  STRING: unknown;
  INTEGER: unknown;
};

export declare class Model {
  static init(attributes: Record<string, unknown>, options: Record<string, unknown>): void;
  static findAll(options?: unknown): Promise<unknown[]>;
  static findByPk(id: string): Promise<unknown>;
  static count(options?: unknown): Promise<number>;
  static create(values: Record<string, unknown>): Promise<unknown>;
  static update(values: Record<string, unknown>, options: unknown): Promise<unknown>;
  static destroy(options: unknown): Promise<number>;
}

export type ModelStatic = typeof Model;

export declare class Sequelize {
  constructor(options?: unknown);
  define(name: string, attributes: Record<string, unknown>): ModelStatic;
  /** Every model the connection knows, by name. Real, and unreadable on purpose. */
  models: Record<string, ModelStatic>;
}
