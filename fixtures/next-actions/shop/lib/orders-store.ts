/** The work the boundaries do, kept away from them so the flow has two hops. */
export const archive = async (id: string): Promise<void> => {
  void id;
};

export const rename = async (id: string, name: string): Promise<void> => {
  void id;
  void name;
};

export const exportAll = async (): Promise<string> => 'orders.csv';
