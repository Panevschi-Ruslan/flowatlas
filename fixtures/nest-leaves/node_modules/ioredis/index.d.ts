// Hand-written `ioredis` stub. The receiver's type being declared here is what
// marks a call as touching the cache; the key is read from the first argument.

export declare class Redis {
  get(key: string): Promise<string | null>;
  set(key: string, value: string, mode?: string, ttl?: number): Promise<'OK'>;
  del(...keys: string[]): Promise<number>;
  expire(key: string, seconds: number): Promise<number>;
}

export default Redis;
