// Hand-written `@nestjs/core` stub: enough for a `main.ts` that boots.

export declare const NestFactory: {
  create(module: unknown, options?: unknown): Promise<{ listen(port: number): Promise<void> }>;
};
