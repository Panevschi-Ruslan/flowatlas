// Hand-written `@nestjs/microservices` stub.
//
// `ClientProxy.emit` and `.send` are what the broker adapter matches a
// publisher on, and it insists the receiver's type is declared in this package,
// so the specifier is the load-bearing part.

export declare class ClientProxy {
  emit<T = unknown>(pattern: unknown, data: T): unknown;
  send<T = unknown, R = unknown>(pattern: unknown, data: T): R;
}

export declare const Transport: { TCP: 'TCP'; KAFKA: 'KAFKA'; RMQ: 'RMQ' };

export declare const ClientsModule: {
  register(
    clients: ReadonlyArray<{ name: string; transport?: unknown; options?: unknown }>,
  ): unknown;
};

export declare function EventPattern(pattern: string | object): MethodDecorator;
export declare function MessagePattern(pattern: string | object): MethodDecorator;
export declare function Payload(property?: string): ParameterDecorator;
