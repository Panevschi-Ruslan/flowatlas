// Hand-written `typeorm` stub, the same shape `fixtures/nest-typeorm` uses. It
// is here for one reason: the far end of the cross-repo chain §12 walks is
// `table:orders#Order`, so this repository has to reach a data layer the way a
// real one does — through a receiver whose type is declared in a database
// package, never through a receiver's name.
//
// `InjectRepository` really lives in `@nestjs/typeorm`; it is folded in here so
// the fixture needs a single stub package. It is a decorator, never a call
// receiver, so no adapter reads it.

export declare class Repository<Entity> {
  find(options?: unknown): Promise<Entity[]>;
  findOne(options?: unknown): Promise<Entity | null>;
  save(entity: Entity): Promise<Entity>;
  delete(id: unknown): Promise<void>;
  createQueryBuilder(alias?: string): unknown;
}

export declare const Entity: (name?: string) => ClassDecorator;
export declare const PrimaryGeneratedColumn: (strategy?: string) => PropertyDecorator;
export declare const Column: (options?: unknown) => PropertyDecorator;
export declare const InjectRepository: (entity: unknown) => any;
