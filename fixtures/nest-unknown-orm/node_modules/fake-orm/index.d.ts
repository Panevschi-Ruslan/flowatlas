// A data layer nobody has written a descriptor for. Its type names the entity
// the way a repository does, so the entity is still readable; what is lost is
// only whether each method reads or writes.

export declare class Repo<Entity> {
  find(where?: unknown): Promise<Entity[]>;
  persist(entity: Entity): Promise<Entity>;
  drop(id: string): Promise<void>;
}
