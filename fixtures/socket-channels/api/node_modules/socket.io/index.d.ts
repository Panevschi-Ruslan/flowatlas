// Hand-written `socket.io` stub: only what the extractor reads.

/** What `to(room)` returns: the same publishing surface, aimed at an audience. */
export declare class BroadcastOperator {
  emit(event: string, ...payload: unknown[]): boolean;
  to(room: string): BroadcastOperator;
  except(room: string): BroadcastOperator;
}

export declare class Socket {
  readonly id: string;
  emit(event: string, ...payload: unknown[]): boolean;
  on(event: string, listener: (...payload: never[]) => void): this;
  join(room: string): void;
  to(room: string): BroadcastOperator;
}

export declare class Namespace {
  emit(event: string, ...payload: unknown[]): boolean;
  to(room: string): BroadcastOperator;
}

export declare class Server {
  emit(event: string, ...payload: unknown[]): boolean;
  to(room: string): BroadcastOperator;
  of(namespace: string): Namespace;
}
