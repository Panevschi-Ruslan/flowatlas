// Hand-written `bullmq` stub for the flowatlas fixture suite. It declares only
// what `fixtures/nest-bullmq/src` touches, in the real library's shape.
//
// `Queue.add(name, data)` is the producer the adapter reads: the channel is the
// queue name, and the job name goes to `producer.meta.jobName`. The constructor
// is declared too, because `new Queue(variable)` is the row where the queue
// token stops being resolvable.

export interface JobsOptions {
  attempts?: number;
  delay?: number;
  priority?: number;
  removeOnComplete?: boolean | number;
}

export interface QueueOptions {
  connection?: { host?: string; port?: number };
  prefix?: string;
}

export declare class Job<DataType = unknown, ReturnType = unknown, NameType extends string = string> {
  readonly id?: string;
  readonly name: NameType;
  readonly data: DataType;
  updateProgress(value: number | object): Promise<void>;
  moveToFailed(error: Error, token: string): Promise<void>;
}

export declare class Queue<DataType = unknown, ReturnType = unknown, NameType extends string = string> {
  constructor(name: string, options?: QueueOptions);
  readonly name: string;
  add(name: NameType, data: DataType, options?: JobsOptions): Promise<Job<DataType, ReturnType, NameType>>;
  addBulk(
    jobs: Array<{ name: NameType; data: DataType; opts?: JobsOptions }>,
  ): Promise<Array<Job<DataType, ReturnType, NameType>>>;
  close(): Promise<void>;
}

export declare class Worker<DataType = unknown, ReturnType = unknown, NameType extends string = string> {
  constructor(name: string, processor?: unknown, options?: QueueOptions);
  close(): Promise<void>;
}
