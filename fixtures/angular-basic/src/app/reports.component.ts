import { Component } from '@angular/core';

/** The screen a loader takes whole, because the module exports it as its default. */
@Component({
  selector: 'app-reports',
  template: '<h1>Reports</h1>',
})
export default class ReportsComponent {}
