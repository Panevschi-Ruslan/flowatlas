import { Component } from '@angular/core';

/** A screen a route loads rather than names, written the ordinary way. */
@Component({
  selector: 'app-settings',
  template: '<h1>Settings</h1>',
})
export class SettingsComponent {}
