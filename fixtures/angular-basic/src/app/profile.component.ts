import { Component } from '@angular/core';

/** The same screen a `then` would have picked, reached by awaiting the import. */
@Component({
  selector: 'app-profile',
  template: '<h1>Profile</h1>',
})
export class ProfileComponent {}
